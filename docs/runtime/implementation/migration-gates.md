# Migration Gates

- **Phase C arbitration:** traces required; golden replay no rubric regression
- **Phase D prompt strip:** plan adherence ≥ model profile threshold
- **Layer C change:** governance manifest + ROI + golden replay
- **Runtime PR:** `npm run test:golden-replay` + `npm run test:runtime-governance`

Kill-switch: adherence collapse ≥2 → `deterministic`. See [runtime-modes.md](../arbitration/runtime-modes.md).
